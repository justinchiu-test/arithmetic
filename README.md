# arithmetic
run
```
# training set of 50k
uv run python -m arithmetic.generate --num_examples 50000
# training set of 20k
uv run python -m arithmetic.generate --num_examples 20000
# test set of 10k
uv run python -m arithmetic.generate --num_examples 10000
```
