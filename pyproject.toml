[project]
name = "arithmetic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.3",
    "strictfire>=0.4.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
